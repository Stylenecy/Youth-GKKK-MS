node_modules
.next
.env.local
.env.*.local
out
dist
build
coverage
*.log
.DS_Store
.idea
.vscode
.vercel

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Importer output: contains REAL phone numbers + birth dates, never commit
scripts/import/output/
__pycache__/

# Raw source data: real names/phone/birth-date/WhatsApp exports, local-only, never commit
database/
Laporan_Kas_Pemuda_2026.xlsx
.agent/Cross/Chat/

# Discarded Canva font-exploration exports (61 MB) — not the final logo,
# kept locally for reference only. See public/logo/BRAND-GUIDE_Youth-GKKK.md.
public/logo/_archive-canva-exports/

# Local tool binaries (ffmpeg, 296 MB) — not project code, download separately.
bin/

# Raw source footage (20 MB) for the landing image-sequence — only the
# extracted frames in public/sequence/ are actually used by FrameSequence.tsx.
# Keep the masters out of git; back up separately (e.g. Drive) if needed.
public/video/

# Local Claude Code permission settings — machine-specific, not shared config.
.claude/settings.local.json

# Stray file from an unrelated project (Elyse Residence, Webflow export) —
# not part of YGMS. Move it to its own repo/folder when you get a chance.
.agent/Idea/elsye-residence.html

# Scratch SVG-render debug harness, not a deliverable.
public/logo/derived/_temp_render.html
